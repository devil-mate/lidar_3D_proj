<?xml version="1.0"?>
<launch>

	<!-- <arg name="config_package" default="$(optenv ROBOT_LOCALIZATION_CONFIG_PACKAGE summit_xl_localization)"/>
	<arg name="config_folder" default="config"/>
	<arg name="config_path" default="$(eval find(config_package) + '/' + config_folder)"/>

	<arg name="id_robot" default="$(optenv ROBOT_ID robot)"/>
	<arg name="prefix" default="$(arg id_robot)_"/> -->
	<arg name="slam_scan_topic" default="$(optenv ROBOT_LOCALIZATION_SCAN_TOPIC scan_filtered)"/>
	<arg name="config_prefix" default=""/>

	<arg name="map_frame" default="map"/>
	<arg name="odom_frame" default="odom"/>
	<arg name="base_frame" default="base_link"/>

  <node pkg="gmapping" type="slam_gmapping" name="slam_gmapping">
    <remap from="scan" to ="$(arg slam_scan_topic)"/>
  	<rosparam command="load" file="$(find summit_xl_localization)/config_$(arg config_prefix)/gmapping_params.yaml" subst_value="true"/>
  	<!-- <rosparam command="load" file="$(find tv_slam)/config/turtebot3_gmapping_params.yaml" subst_value="true"/> -->
  </node>

</launch>
